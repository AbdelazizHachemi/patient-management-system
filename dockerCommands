docker run -d --name billing-service --add-host=host.docker.internal:host-gateway -p 4001:4001 -p 9001:9001 --network patient-network billing-service:local 

docker run -d \
  --name patient-service \
  --network patient-network \
  -p 4000:4000 \
  -e SPRING_DATASOURCE_URL=jdbc:postgresql://host.docker.internal:5000/patientdb \
  -e SPRING_DATASOURCE_USERNAME=postgres \
  -e SPRING_DATASOURCE_PASSWORD=postgres \
  -e SPRING_JPA_HIBERNATE_DDL_AUTO=update \
  -e SPRING_SQL_INIT_MODE=always \
  -e SERVER_PORT=4000 \
  -e BILLING_GRPC_HOST=billing-service \
  -e BILLING_GRPC_PORT=9001 \
  patient-service

  docker exec patient-service printenv | grep BILLING_GRPC